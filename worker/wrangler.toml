name = "mit-collabs-agent"
main = "index.js"
compatibility_date = "2026-07-01"

# Secret (set via `wrangler secret put ANTHROPIC_API_KEY`, never in this file):
#   ANTHROPIC_API_KEY

# Optional override if you ever move the summary.json to a different URL:
# [vars]
# SUMMARY_URL = "https://academic-analytics-llc.github.io/mit-collabs/data/agent/summary.json"
